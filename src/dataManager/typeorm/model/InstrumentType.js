class InstrumentType {
    constructor(id, instrument, type) {
        this.id = id;
        this.instrument = instrument;
        this.type = type;
    }
}

module.exports = {
    InstrumentType: InstrumentType
};